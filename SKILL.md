---
name: 1688-ecom-image-pipeline
description: Unified 1688 B2B ecommerce image workflow combining product facts, 1688 conversion structure, reusable ecommerce image templates, Campaign Style Lock, CodesOnline image generation, deterministic text/layout, and final mobile/compliance QA.
---

# 1688 电商图片整合工作流

这个 Skill 是当前项目的统一入口。它把三个职责串起来：

- 1688 B2B 策划：采购逻辑、事实边界、主图/详情页分镜、中文文案和合规验收；
- 电商图片模板：场景模板、Prompt 结构、批量图片包和 Campaign Style Lock；
- CodesOnline：实际生成或编辑位图，并保存到当前工作目录。

## 规则优先级

当规则冲突时，按以下优先级处理：

1. 用户当前明确要求；
2. 真实产品素材和用户确认的产品资料；
3. 1688 B2B 事实、采购转化和合规规则；
4. 本 Skill 的输出与验收流程；
5. 电商图片模板的风格和构图建议。

美观不能覆盖真实性。未知信息写“待确认”，不能变成卖点、参数或承诺。

## 适用范围

默认用于 1688 工厂店：

- 商品主图；
- 详情页长图；
- 工艺、材质、规格和包装图；
- 定制能力、MOQ、交期和工厂信任图；
- 店铺 Banner 或品牌介绍图。

不默认制作零售平台式的夸张促销图，也不默认虚构认证、销量、产能、评价或检测结果。

## 标准工作流

### 1. 建立产品事实表

先从用户确认、原始产品照片、规格表、证书或检测材料中提取信息，并给每条信息标注来源：

- 已确认；
- 原始素材；
- 证据文件；
- 待确认。

必须区分：

- 可以裁切、抠图、换背景、补光的内容；
- 产品外观、纹理、颜色、数量、比例、图案、包装和参数等不可擅自改动的内容。

### 2. 输出 1688 页面策略

在生图前先确定：

- 目标采购商和采购任务；
- 商品类别、价格/MOQ/规格/交期是否已确认；
- 工厂直供、定制、样品、品控和交付的真实证据；
- 主图和详情页每张图的职责；
- 每张图需要的真实产品素材和证据来源；
- 禁止表达和缺失资料。

默认采购信息优先级：

1. 产品是什么、是否可定制；
2. MOQ、价格、规格或交期；
3. 材质、工艺和真实产品细节；
4. 样品、品控、包装和交付证据；
5. FAQ、适用范围和行动指引。

### 3. 建立 Campaign Style Lock

多图任务必须锁定以下内容：

- 2–3 个主色 + 1 个强调色；
- 冷暖调；
- 背景系统；
- 光线方向和阴影强度；
- 字体风格；
- 产品比例和展示角度规则；
- 信息图、图标、标签和编号样式；
- 禁止色板漂移、字体混用、随机背景和不一致光线。

1688 B2B 项目默认使用清晰、克制、便于采购扫描的风格。不得为了“高级感”牺牲产品识别、规格阅读或 MOQ 可见性。

### 4. 选择图片模板并生成 Prompt

优先复用已安装 `ecom-details-image` 的 `references/templates/`。只读取当前任务需要的模板，不一次性加载全部模板。

处理键帽现货、键位图、热升华工艺、客户定制案例、阶梯报价或生产质检页面时，先完整阅读 [references/keycap-1688-image-template.md](references/keycap-1688-image-template.md)。用该模板拆分现货与定制事实、选择 M01–M05/D01–D04 页面结构，并执行开放式版式和物理一致性验收；模板中的字段是占位结构，不得替代当前用户确认的事实。

不熟悉 Agent 的同事先阅读 [references/agent-quickstart.md](references/agent-quickstart.md)，按“新手六步”、决策表、交接模板和验收回报格式执行。`codesonline-image` 是外部图像生成的独立 companion skill；`ecom-details-image` 的模板为可选依赖，缺失时仍可使用本技能内置的页面职责和键帽模板。

常用映射：

- 主图 → `01-hero-image.json`；
- 场景图 → `02-lifestyle-scene.json`；
- 细节/材质/工艺 → `04-detail-macro.json`；
- 包装 → `10-packaging.json`；
- 信息图/详情页 → `11-infographic.json`；
- 尺寸/规格/步骤 → `13-size-spec.json`；
- 多品/SKU → `14-multi-product.json`；
- 结构拆解 → `17-exploded-view.json`；
- 多角度 → `19-multi-angle-grid.json`；
- 店铺首页 → `24-storefront.json`。

每个 Prompt 必须包含：主体、用途、构图、镜头角度、产品占比、留白、光线、色彩、真实素材限制、平台画幅和负面约束。

### 5. 图片生成与文字分层

#### 子技能与子 Agent 协作

- 先由主 Agent 识别并读取所有触发的技能；不要把技能规则的阅读、解释或事实锁定完全委托给子 Agent。需要 `codesonline-image` 时，主 Agent 先读取其 `SKILL.md` 及当前任务所需的参考文件，再给出调用参数。
- 对不熟悉 Agent 的同事，优先使用 `references/agent-quickstart.md` 中的固定流程和交接模板；不要凭记忆临时拼接任务消息。
- 用户要求并行制作多张独立图片时，可以为每个页面或素材组启动一个子 Agent。交接信息必须包含：真实素材绝对路径、输出绝对路径、已确认事实、Campaign Style Lock、画幅/尺寸、禁止修改项、禁止文案和验收标准。
- 子 Agent 只负责交接范围内的生成或排版，不得擅自修改共享事实表、产品路径、价格、MOQ 或其他页面；完成后必须回报输出路径、实际尺寸、脚本/调用方式和 QA 结果。
- 主 Agent 必须在所有子 Agent 完成后统一查看成图，复核产品真实性、物理关系、尺寸、禁用信息和移动端可读性；不能只依据子 Agent 的文字报告交付。
- 外部图像服务上传真实产品照片前，必须由主 Agent 在当前根任务中取得用户对具体文件的明确授权。子 Agent 不能把主 Agent 转述的授权当作直接授权；若授权上下文无法继承，由主 Agent 直接调用外部服务或重新取得授权后再执行。
- 子 Agent 生成的文字、价格、MOQ、交期和质检数据仍须由主 Agent 确定性后期排版和复核，不能把模型生成的文字当作最终商品信息。

如果用户明确要求使用 CodesOnline：

1. 由本 Skill 生成最终 Prompt；
2. 使用 `codesonline-image` 的脚本调用 CodesOnline；
3. 主图默认使用 `1:1`，详情页默认使用 `2:3`，除非用户指定平台尺寸；
4. 有产品照片时使用参考图参数；
5. 产品图尽量使用原始素材或参考图，不允许 AI 擅自重绘关键外观；
6. 生成后检查文件非空、画幅、产品完整性和明显变形。

AI 只负责生成以下内容：

- 无文字背景；
- 桌搭、场景和氛围；
- 可验证产品照片的环境适配；
- 不含最终参数的 UI、图标或信息图底稿。

#### 产品真实性与物理一致性（硬约束）

当画面包含用户提供的实拍产品时，以下规则优先于任何美化、桌搭或生成建议：

- 不得改变产品本体的字符、图案、颜色、纹理、材质观感、数量、比例、形状、相对位置或结构；不得补画、替换、删除或生成新的产品细节。
- 键帽等带有印刷图案的产品，禁止 AI 重绘、锐化生成式细节或添加不存在的表面纹理。需要突出材质时，优先使用用户提供的真实微距图或同一实拍中的原始裁切。
- 保持产品与原始实拍一致的主光方向、光照强度、色温、接触阴影和支撑关系。若只替换产品以外的背景，新背景的光线、投影、透视和接触面必须与产品原图相容。
- 新增的实体道具或场景元素必须有合理物理支撑，并与原图具备一致的光照方向、强度、投影和重力关系；无法可靠做到时，宁可使用干净背景，不得添加看起来悬浮、错光或材质不自然的道具。
- 强制验收句：**确保键帽在桌面上有自然的接触阴影，新增元素的光照方向、强度、投影关系与原图完全一致，且具备合理物理支撑（如热源、接触面、悬挂点、重力方向）。**
- 放大圆窗、引线、文字等属于信息图叠加，可不模拟实体投影；但放大内容必须取自真实产品局部，引线须准确指向对应的真实部位。
- 生成或后期完成后，必须逐项复核：产品细节未变、光影一致、接触阴影自然、背景无不合物理规律的支撑/悬浮问题、未新增无依据纹理。

以下内容必须由确定性排版或人工后期完成：

- 中文标题；
- 价格、MOQ、尺寸、单位和交期；
- 认证、检测、质检和工厂数据；
- Logo、商标和品牌字；
- 关键箭头、尺寸线和结构标注。

### 6. 默认图片包

除非用户指定其他数量，1688 商品图片包采用可调整的结构：

- M01 主图：产品 + 品类/核心采购钩子；
- M02 主图：材质或工艺细节；
- M03 主图：使用或应用场景；
- M04 主图：规格、SKU 或定制选项；
- M05 主图：包装、交付或信任证据；
- D01 详情首屏：产品为谁解决什么采购问题；
- D02 详情卖点：核心功能/工艺；
- D03 详情规格：尺寸、材质、接口或适配范围；
- D04 详情定制：颜色、图案、Logo 或打样流程；
- D05 详情流程：下单、打样、生产、质检、交付；
- D06 详情证据：已有包装、质检或真实工厂资料；
- D07 详情 FAQ：适用范围、MOQ、交期和行动指引。

不强行补齐缺失资料；没有证据的页面写“待确认”或保留占位区。

### 7. 最终验收

同时进行四类检查：

- 真实性：产品外观、材质、颜色、数量、比例和包装是否与原始素材一致；
- 1688 转化：产品、价格/MOQ、规格和采购行动是否清晰；
- 移动端：缩小到约 200 px 后，产品和关键采购信息是否仍可识别；
- 合规：是否出现无证据的“最强、第一、绝对”、认证、销量、产能、交期或效果承诺。

中文、数字和单位必须放大核对；生成模型出现乱码时必须改为后期排版，不能直接交付。

## 输出格式

Brief 模式输出：

1. 产品事实与待确认项；
2. 1688 转化目标；
3. Campaign Style Lock；
4. 图片包和每张图的页面职责；
5. 每张图的 Final Image Prompt；
6. 确定性排版清单；
7. 禁止表达和验收清单。

Generate 模式额外输出：

1. 生成使用的参数；
2. 生成文件绝对路径；
3. 生成后发现的问题；
4. 是否需要重生成或后期排版。

## API 安全

只从 `CODESONLINE_IMAGE_API_KEY` 读取 CodesOnline 密钥。不得把密钥放入 Prompt、文件、日志或最终回复。没有密钥时只输出完整 Prompt 和配置说明，不声称已经出图。
